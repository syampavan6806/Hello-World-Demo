name: hello-world-demo
on:
  push:
    branches:
        - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: hello world demo
        run: |
            echo "Hello, World! This is a demo workflow." > hello.txt 
            date
            ls -la
            whoami
            pwd
            cat hello.txt
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test deploy
        run: |
            echo "Deploying the application..." 
            date
            df -kh
  scan:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test scan
        run: |
            echo "Running security scan..." 
            date
            